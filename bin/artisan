#!/usr/bin/env bash
# php artisan dins d'admin o api
# ús: ./bin/artisan api migrate   |   ./bin/artisan admin route:list
set -euo pipefail
cd "$(dirname "$0")/../docker/php-infraestructura"
target="${1:?cal indicar admin|api}"
shift
podman compose --env-file .env exec "$target" php artisan "$@"
